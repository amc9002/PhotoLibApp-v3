import { Routes } from '@angular/router';
import { GalleryListComponent } from './components/gallery-list/gallery-list.component';

export const routes: Routes = [{ path: '', component: GalleryListComponent }];
